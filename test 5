#set
a = {1, 11, 'a', (1, 11), None, True, print} # множество в котором: целое; вещественное; строковое; кортеж; пустой тип; булевый тип; встроенная функция.
a = {1, 1.1,[1, 2]} # обмануть встроенные проверки добавлением списка в кортеж не получится, возникнет ошибка
# TypeError: unhashable type: 'list'


#множество set
a = {1, 2, 3}
b = {3, 5, 6}
c = a - b
print(c) # {1, 2} так как из 1, 2, 3 только 1 и 2 нет во множестве "b"
a -= b # теперь "a" равно {1, 2}
